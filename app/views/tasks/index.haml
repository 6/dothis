%section#todoapp
  %header
    %input#new-todo{:placeholder => "What needs to be done?", :autofocus => true}
  %section#main
    %ul#todo-list
  %footer#footer

:javascript
  var app = app || {};
  var ENTER_KEY = 13;

  $(function () {
    app.User = new app.User(#{current_user.as_json.to_json});
    new app.AppView();
  });
